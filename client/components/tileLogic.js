import React, { Component } from "react";
import { createBlankTile, createCube } from "./renderFuncs/createTile";

export const tileLogic = initialCube => {
  let tile1 = createBlankTile(null, 0, 1);
  let tileRandom = {
    region: "monastery",
    edges: [],
    hasShield: false,
    meeple: []
  };
  road, [right, left], false), new Region(city, [top], false)], 0)

  let startTile = [{
    region: "road",
    edges: [1, 3],
    hasShield: false,
    meeple: []
  },
  {
      region: "city",
      edges: [0],
      hasShield: false,
      meeple: []
  }]

  // write tileLogic function pass into addCube in Board

  return tile1;
};

export default tileLogic;
